steps:
  # Step 1: Build the frontend container image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'northamerica-northeast2-docker.pkg.dev/$PROJECT_ID/sofiarahul-docker-repo/frontend:latest', './frontend']

  # Step 2: Push frontend image to Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'northamerica-northeast2-docker.pkg.dev/$PROJECT_ID/sofiarahul-docker-repo/frontend:latest']

  # Step 3: Deploy frontend to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args: [
      'run', 'deploy', 'sofiarahul-frontend',
      '--image', 'northamerica-northeast2-docker.pkg.dev/$PROJECT_ID/sofiarahul-docker-repo/frontend:latest',
      '--region', 'northamerica-northeast2',
      '--allow-unauthenticated'
    ]

options:
  logging: CLOUD_LOGGING_ONLY